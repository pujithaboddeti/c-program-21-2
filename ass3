#include <stdio.h>
int main()
{
    int s=0,n,i;
    printf("enter a number:");
    scanf("%d",&n);
    while(n>0)
    {
        i=n%10;
        s=(s*10)+i;
        n=n/10;
    }
    printf("%d the reverse number",s);
}
